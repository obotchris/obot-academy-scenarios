# Set Yourself as Owner

The first user to log in will need to be set as **Owner**

Log in using your Google account and when prompted set yourself as **Owner**

> **Heads up:** the Google sign-in cannot load inside the embedded browser on the right — Google's frame policy blocks it. Click **Open in New Tab** (top-right of the environment panel) to open Obot in its own tab, then sign in with Google and set yourself as **Owner** there. Once you've done that, return here and **refresh** the page to continue.

Anyone with a valid email can now log in to your Obot instance, you can administer the roles that they have once their accounts have been created in the **Users** section