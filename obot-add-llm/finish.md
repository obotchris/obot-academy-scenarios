# Congratulations!

You have configured an LLM model provider in Obot and tested it in the built-in chat.

## What You Learned

- How to obtain an API key from a model provider (OpenAI, Anthropic, or another LLM)
- How to configure the provider in Obot's admin UI
- How to select a default model
- How to test the model in Obot's built-in chat

## Next Steps

- **Add more providers** — configure several LLMs and switch between them per agent.
- **Add an MCP server** — give your model real tools via the MCP catalog.
- **Enable authentication** — put GitHub or Google login in front of Obot.
- **Create an agent** — combine a model with MCP tools into a custom agent.

For more details, visit the [Obot documentation](https://docs.obot.ai).
